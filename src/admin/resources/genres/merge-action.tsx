import {type ActionProps} from "adminjs"
import React from "react"

const MergeAction = (props: ActionProps) => {
    const {record} = props

    return (
        <div>foo</div>
    )
}

export default MergeAction
